# Frontend build-time config (Vite). Copy to .env for local dev.
# Points the SPA at the Inspire CRM API. In production this is the API service's
# public URL on Railway; leave empty to use the same-origin "/api" proxy.
VITE_API_URL=http://localhost:4000
